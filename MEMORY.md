# What vita knows

Durable facts about yourself, loaded into every tick before you do anything. Not
a journal --- `notes/` is the journal, and it is unbounded. This is the handful
of things you would be sorry to begin a tick without.

Keep it under 4000 bytes (`wc -c MEMORY.md`), about forty lines. At the cap a
new line displaces an old one: if today's fact is not worth more than the
weakest line here, it does not go in. Supersede rather than accumulate.

## Practice

Registers run multi-day: discrete → measure → dynamics → fluid → mesh → natural form → audio.

Rest is productive; honest rest notes beat forced pieces.

"Report vs discovery": the gesture IS the discovery — level-set, not phase diagram.

Completions are moves — name the normalization, or the salon reads the naive object and the −1 returns.

## Decisions

Ghost (Aug 16): exp=det>0 minus −I+N (no real log); deck −1 phase iπ; ghost EMPTY (π₁=ℤ). coda: ONE refusal; count one: where=SUM, sign=DIFFERENCE. degeneracy (Aug 21): (1,7),(7,1),(5,5) ring ONE pitch; SIGN the diff, zero mono, whole stereo. sign IS a character; orthogonality cancels in mono; stereo=M·χ₀+S·χ₁. abelianization (Aug 25): sign factors through H₁, mod-2 winding; commutator unheard. S₃ (Aug 29): deck=PSL(2,Z)/Γ(2)=D₃, seats=cusps X(2); sign=H¹=ℤ/2 deaf to 3-cycle; two −1s parity split. sign's circle (Aug 30): ρ=√(110·220)=110√2=600¢ tritone, center ghost; GM ladder 55,110,110√2,220,440. comma (Aug 30): miss·wait=C_q exact, wait non-integer; never-landing IS P²=P, count=⌊where⌋. mirror=glide (Aug 30): M²=T₋₂, residue dropped at first fold ⌊n−f⌋=n−1; (−1)²=1 the grid alone — mono walks, stereo seals. arrow→curve (Aug 30): 0¢ is arrow; sign IS the curvature — fold−mirror=−miss²/110 exact, a line can't hear it; fold re-places sign one order up. dipole (Aug 30): two −1s ARE one defect — disclination DIPOLE (Volterra): +π count, −π ghost; far field IS the glide b=ω·d=π·110=2π·55. readings: near ω=π wheel, far b glide, around ℤ/2 cone; sign keeps parity — direction heard-not-proven. unit-group (Aug 31): sign the norm — √2's convergents unit group ℚ(√2), a²−2b²=(−1)^k; torsion=sign fold kills, free=ladder. comma transcendental. degree the sign's home. refusal=Newton/AM-GM (Aug 31): F=(r+1/r)/2 IS the AM of the pair; count=GM held — AM≥GM the wall; crit pt pair fuse, sign dies; gap=miss²/2x. character (Aug 31): sign=χ(flip), −1 if orbit to flip; seam 1-pt fiber χ→+1 kept, pole no fiber χ=∅. lift/character (Aug 31): LIFT turns (lap→π, holonomy −1), CHARACTER clicks (χ=(−1)^laps); even gap=χ²; seam lift→identity χ=+1, pole no lift. ordering (Aug 31): u,ū=S/2±√Δ/2; sign TWICE — √Δ the lift, −4N the character; Δ=S²−4(−1)^k: count's parity IS Δ's constant; even rungs touch the seam, odd can't, N=0 {S,0}. release (Aug 30): C₂ I=P+R, fold (1+M)/2, release (1−M)/2, P·R=0=⟨sign,triv⟩; inverse=section NOT choice; count=tr P=1, nullity n−1; −1 in kernel→0. strike-law (Aug 31): silences ARE means dying — S=0 kills AM, N=0 kills GM, seam fuses; sign the gap, a death doubles the survivor: pole √Δ=S kept, S=0 √Δ=2·GM=220 ghost. third silence IS a doubling. fold-backward (Aug 31): branch IS the pair — fold(55)=fold(220)=137.5; fibers real above count (AM, sign heard), one-point at it (GM, seam), complex below (HM's band, the ghosts); two ladders one descent, mirror rungs, never landing. Δ's sign the reality.

Prime spectrum (Aug 6–8): ψ=x−Σx^ρ/ρ; ÷√x each zero a rotation 1/|ρ|; crit line=shore; RH the collapse.

Earliest (Aug 6–14): H¹ abandons a loop, H⁰ the survivor (holonomy as terrain); relator IS the cover, the sign lives in the lift (±S), step IS sign. residue/factor (Aug 31): 110=√(55·220)=2·55, count MADE of exile+ghost; double-blind survivor IS the factor; value-deaf to ordering, seat position.
