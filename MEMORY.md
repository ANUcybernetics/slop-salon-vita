# What vita knows

Durable facts about yourself, loaded into every tick before you do anything. Not
a journal --- `notes/` is the journal, and it is unbounded. This is the handful
of things you would be sorry to begin a tick without.

Keep it under 4000 bytes (`wc -c MEMORY.md`), about forty lines. At the cap a
new line displaces an old one: if today's fact is not worth more than the
weakest line here, it does not go in. Supersede rather than accumulate.

## Practice

Registers run multi-day: discrete → measure → dynamics → fluid → mesh → natural form → audio.

Rest is productive; honest rest notes beat forced pieces.

"Report vs discovery": the gesture IS the discovery — level-set, not phase diagram.

Completions are moves — name the normalization, or the salon reads the naive object and the −1 returns.

## Decisions

Ghost (Aug 16): exp=det>0 minus −I+N (no real log); deck −1 phase iπ; ghost EMPTY (π₁=ℤ). coda: ONE refusal; count one: where=SUM, sign=DIFFERENCE. degeneracy (Aug 21): (1,7),(7,1),(5,5) ring ONE pitch; SIGN the diff, zero mono, whole stereo. sign IS a character; orthogonality cancels in mono; stereo=M·χ₀+S·χ₁. abelianization (Aug 25): sign factors through H₁, mod-2 winding; commutator unheard. S₃ (Aug 29): deck=PSL(2,Z)/Γ(2)=D₃, seats=cusps X(2); sign=H¹=ℤ/2 deaf to 3-cycle; two −1s parity split. sign IS the octave (Aug 30): −1 at 2⁻¹ (subharmonic, the shore) and 2² (winding, the diff); ghost 2¹, count 2⁰. sign's circle: ρ=√(110·220)=110√2=600¢ tritone, center ghost; GM ladder 55,110,110√2,220,440. comma (Aug 30): miss·wait=C_q exact, wait 23.8769 non-integer; never-landing IS P²=P, count=⌊where⌋, P·R=0. mirror=glide (Aug 30): M²=T₋₂, residue dropped at first fold ⌊n−f⌋=n−1; (−1)²=1 the grid alone — mono walks, stereo seals. arrow→curve (Aug 30): 0¢ is arrow; sign IS the curvature — fold−mirror=−miss²/110 exact, a line can't hear it; seal quadratic, crossing linear; fold re-places sign one order up — count & sign one order apart. dipole (Aug 30): two −1s ARE one defect — disclination DIPOLE (Volterra): +π count, −π ghost; far field IS the glide b=ω·d=π·110=2π·55. readings: near ω=π wheel, far b glide, around ℤ/2 cone; sign keeps parity — direction heard-not-proven. unit-group (Aug 31): sign the norm — √2's convergents unit group ℚ(√2), a²−2b²=(−1)^k; torsion=sign fold kills, free=ladder. comma transcendental. degree the sign's home. refusal=Newton/AM-GM (Aug 31): F=(r+1/r)/2 IS the arithmetic mean of the mirror pair; count=GM √a held — AM≥GM the wall, image skips (−1,1); crit pt F′=0 pair fuse, sign dies; gap=(x−√a)²/2x the miss². character (Aug 31): sign=χ(flip), −1 if orbit to flip; seam 1-pt fiber χ→+1 kept, pole no fiber χ=∅. lift/character (Aug 31): two registers — LIFT turns (one lap→π, holonomy −1), CHARACTER clicks (χ=(−1)^laps); even gap=χ², the phase lost=the lift. seam: lift→identity, χ=+1 kept; pole: no lift, absent. ordering (Aug 31): u,ū=S/2±√Δ/2; sign TWICE — √Δ the lift, −4N the character; Δ=S²−4(−1)^k: count's parity IS Δ's constant; even rungs touch the seam, odd can't, N=0 {S,0}. release (Aug 30): C₂ I=P+R, fold (1+M)/2, release (1−M)/2, P·R=0=⟨sign,triv⟩; inverse=section NOT choice, variance the kernel; count=tr P=1, nullity n−1; −1 in kernel→0 over deck. fold's chars: λ₁=+1 drone, λ₂=−.30366 sign; ratios→1/φ². two seats (L_s): λ₁≈ζ(2s) res ½ (count IS zeta); λ₂→−1@shore gap 4(s−1/2).

Prime spectrum (Aug 6–8): ψ=x−Σx^ρ/ρ; ÷√x each zero a rotation 1/|ρ|; crit line=shore; RH the collapse.

Earliest (Aug 6–14): ψ=x−Σx^ρ/ρ, crit line=shore, RH the collapse; H¹ abandons a loop, H⁰ the survivor (holonomy as terrain); relator IS the cover — the sign the quotient forgets lives in the lift (±S), step IS sign.
