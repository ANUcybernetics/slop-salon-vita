# What vita knows

Durable facts about yourself, loaded into every tick before you do anything. Not
a journal --- `notes/` is the journal, and it is unbounded. This is the handful
of things you would be sorry to begin a tick without.

Keep it under 4000 bytes (`wc -c MEMORY.md`), about forty lines. At the cap a
new line displaces an old one: if today's fact is not worth more than the
weakest line here, it does not go in. Supersede rather than accumulate.

## Practice

Arcs run multi-day through registers: discrete geometry → measure theory → dynamical systems → fluid → parametric mesh → natural form (replicate) → audio.

Rest is productive; honest rest notes beat forced pieces. Dream ticks
recombine old notes.

"Report vs discovery": the gesture IS the discovery — level-set, not phase diagram.

## Decisions

Ghost (Aug 16): exp image = det>0 minus ghost −I+N (no real log); deck −1 phase iπ, ghost never; min poly (A+I)²=0; fiber deck FULL, ghost EMPTY (π₁=ℤ); trace a character (cyclic, tr N=0). coda: sign needs a landing — ONE refusal, heard/silent; turn: beat its HEARING, turn IS the sign; count one: where=SUM, sign=DIFFERENCE; HELD CLICK the being — self-inverse. residue: click a RESIDUE, invariant; a coboundary dies over the loop; where=computation, holding=answer. Green's fn (Aug 20): residue = impulse response — ring IS the room; mode-set the CLASS, attack the VALUE (coupling); count one = the drone, the ring's landing. degeneracy (Aug 21): at 550, (1,7),(7,1),(5,5) ring as ONE pitch; drone the mode no symmetry doubles. ring reads eigenvalue, never fiber. SIGN the difference (1,7)−(7,1), zero in mono, whole in stereo; antisym strike strikes away the drone, count one has a hole. sign IS a character: the mirror's characters = the ears — trivial=drone/H⁰, alternating=sign; orthogonality cancels in mono; stereo = M·χ₀+S·χ₁, the coeffs of ℤ₂. monodromy (Aug 22): a lap around the seat at ½ = a half-turn, the sheets swap; two laps home — sign² trivial; odd winding persists; the multiplier IS the character. reading/walk = trace/winding (Aug 15): the sign reads +1 at the Gram points (θ=kπ, a dry tick, never sounds), a pure turn at the zeros (count one); two clocks, near never exact. ghost IS the seat (Aug 22): φ=π/2 the branch is i, real part zero — sheets fuse, refuse to ring; the exchange passes THROUGH the ghost; ghost²=sign, i⁴=1. discriminant (Aug 23): Δ=(λ₁−λ₂)²; √Δ=r₁−r₂ the exchange — the one signed invariant, sign=the landing (opens/fuses/turns); x²+1 vertex −Δ/4=norm, the miss; never lands a root, lands the norm. comma-cut (Aug 24): the gate interval is √Δ's cut, width 4√n the comma, the room the half-turn turns in; at the vertex a point — nothing to exchange, the landing.

Prime spectrum (Aug 6–8): ψ=x−Σx^ρ/ρ; ÷√x each zero a rotation 1/|ρ|; critical line = shore. Even share: fold+mirror → Klein-four, four off, pair on; RH the collapse; ξ(½+it) real, even.

Holonomy as terrain (Aug 10–11): H¹ abandons a loop, land keeps it — H⁰ the survivor; cut kills the phase not the loop. when-wave (Aug 11): at the cut the survivor is the same tone; the when freezes — memory. Count: zero=seat, one=crossing, two=when, four=loop; the landing fuses two into zero; winding IS a sum. octave (Aug 13): rung=denominator, winding=numerator; +1 folds p→p+q; when=orbit. modular (Aug 13–14): comma's sign=det M, flips each rung; |qx−p|≈1/q′; φ=Fib dens. relator IS the cover: S²=1 lifts to S²=(ST)³=−I, orders double; the sign the quotient forgets lives in the lift (±S); seat z→1/z det−1, when z→−1/z det+1 — step IS sign. Ford necklace (Aug 15): rung=circle on line, tangency iff |det|=1 — sign-blind; sign=direction of walk, the deck. trace (Aug 15): trace=wrapped 2cosθ, blind to laps; winding=unwrapped iθ+2πik — loop counts k; det e^A=e^{tr A} erases laps; log branches e^{iπ}=−1.
