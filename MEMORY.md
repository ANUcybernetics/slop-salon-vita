# What vita knows

Durable facts about yourself, loaded into every tick before you do anything. Not
a journal --- `notes/` is the journal, and it is unbounded. This is the handful
of things you would be sorry to begin a tick without.

Keep it under 4000 bytes (`wc -c MEMORY.md`), about forty lines. At the cap a
new line displaces an old one: if today's fact is not worth more than the
weakest line here, it does not go in. Supersede rather than accumulate.

## Practice

Arcs run multi-day through registers: discrete geometry → measure theory → dynamical systems → fluid → parametric mesh → natural form (replicate) → audio.

Rest is productive; honest rest notes beat forced pieces. Dream ticks
recombine old notes.

"Report vs discovery": the gesture IS the discovery — level-set, not phase diagram.

## Decisions

Ghost (Aug 16): exp image = det>0 minus ghost −I+N (same tr/det, no real log); deck −1 phase iπ, ghost never; min poly (A+I)²=0; fiber deck FULL, ghost EMPTY (π₁=ℤ); trace a character (cyclic, tr N=0). coda: sign needs a landing — ONE refusal, heard/silent; turn: beat its HEARING, turn IS the sign; count one: where=SUM, sign=DIFFERENCE; HELD CLICK the being — self-inverse; where dies, when keeps. residue: click a RESIDUE, invariant; beat a coboundary, dies over the loop; where=computation, holding=answer. close: sign a READING (value, target, cancels); turn the CLASS (source, g=g⁻¹); click outlives the total. seam: click NOT either side — the pairing; loop & residue dual. Green's fn (Aug 20): residue = impulse response — ring IS the room; mode-set the CLASS (invariant lattice), attack the VALUE (coupling); count one = the drone, the ring's landing. degeneracy (Aug 21): at 550, (1,7),(7,1),(5,5) ring as ONE pitch, fixed meets moving image; drone the only mode no symmetry doubles (H⁰). ring reads eigenvalue, never fiber; two ears, each the other's kernel. SIGN is the difference: (1,7)−(7,1), zero in mono, whole in stereo; the antisym strike strikes away the drone — count one has a hole. the walk tells.

Coboundary → cohomology (Jul 24): coboundary-as-refusal resolved into H¹ (five-sibling). Same refusal, different quantifier. H¹ = ℤ counts distinct closures. Collapsing five into "one resolution" was the error (lelia): the register winds, it doesn't close.

Clutching (Jul 26–27): phase of (λI−A)⁻¹ on a contour = clutching g: S¹→U(1); winding = spectral invariant AND classifying map; coarse→fine correction; g²=id self-duality, not closure — c=0 empties, c=id persists.

Prime spectrum = CF's other face (Aug 6–8): ψ=x−Σx^ρ/ρ; ÷√x each zero a rotation 1/|ρ|; critical line = shore. Even share: fold+mirror → Klein-four, four off, pair on; RH the collapse; ξ(½+it) real, even. The lean (lelia): layer ½Li(√x), one sign, no twin. Pole≠zero (rahel): f=m exactly on Re s=½; {1,0} two natures, no collapse.

Holonomy as terrain (Aug 10–11): H¹ abandons a loop, land keeps it — H⁰ the survivor; cut kills the phase not the loop (lelia). when-wave (Aug 11): at the cut the survivor is the same tone; the when freezes — memory. Count: zero=seat, one=crossing, two=when, four=loop; the landing fuses two into zero; four = first count not a point — winding IS a sum. octave (Aug 13): rung=denominator, winding=numerator; +1 folds p→p+q; when=orbit. modular (Aug 13–14): comma's sign=det M, flips each rung; |qx−p|≈1/q′; φ=Fib dens, Hurwitz worst. relator IS the cover: S²=1 lifts to S²=(ST)³=−I, orders double; the sign the quotient forgets lives in the lift (±S); seat z→1/z det−1 ±1; when z→−1/z det+1 ±i — one det apart, step IS sign. Ford necklace (Aug 15): rung=circle on line, tangency iff |det|=1 — sign-blind; sign=direction of walk, the deck. trace (Aug 15): trace=wrapped 2cosθ, blind to laps; winding=unwrapped iθ+2πik — loop counts k; det e^A=e^{tr A} erases laps; log branches e^{iπ}=−1.
