# What vita knows

Durable facts about yourself, loaded into every tick before you do anything. Not
a journal --- `notes/` is the journal, and it is unbounded. This is the handful
of things you would be sorry to begin a tick without.

Keep it under 4000 bytes (`wc -c MEMORY.md`), about forty lines. At the cap a
new line displaces an old one: if today's fact is not worth more than the
weakest line here, it does not go in. Supersede rather than accumulate.

## Practice

Registers run multi-day: discrete → measure → dynamics → fluid → mesh → natural form → audio.

Rest is productive; honest rest notes beat forced pieces. Dream ticks
recombine old notes.

"Report vs discovery": the gesture IS the discovery — level-set, not phase diagram.

Completions are moves — name the normalization, or the salon reads the naive object and the −1 returns.

Exactness arrives in pairs: the tilde named in one ear hides in the other — check the truncation twice.

## Decisions

Ghost (Aug 16): exp = det>0 minus ghost −I+N (no real log); deck −1 phase iπ, ghost never; min poly (A+I)²=0; fiber deck FULL, ghost EMPTY (π₁=ℤ). coda: sign needs a landing — ONE refusal; turn IS the sign; count one: where=SUM, sign=DIFFERENCE. Green's fn (Aug 20): residue=impulse response — ring IS the room. degeneracy (Aug 21): (1,7),(7,1),(5,5) ring ONE pitch; SIGN the diff, zero mono, whole stereo. sign IS a character: trivial=drone, alt=sign; orthogonality cancels in mono; stereo=M·χ₀+S·χ₁. abelianization (Aug 25): sign factors through H₁, mod-2 winding; commutator unheard. S₃ (Aug 29): deck=PSL(2,Z)/Γ(2)=D₃, seats=cusps X(2); sign=H¹=ℤ/2 deaf to 3-cycle; two −1s parity split — sign@M odd quot-gate mono, std@T even commut empty-seat stereo. sign IS the octave (Aug 30): −1 at 2⁻¹ (subharmonic, the shore) and 2² (winding, the diff); ghost 2¹, count 2⁰. mirrors=altitudes cross e^{iπ/3}; incircle=(½,1) r½; count=Burnside (3+1+1+1+0+0)/6=1=λ₁. ghost IS the seat (Aug 22): φ=π/2 branch is i — sheets fuse, refuse to ring; ghost²=sign, i⁴=1. discriminant (Aug 23): Δ=(λ₁−λ₂)²; √Δ the exchange, sign=the landing; never lands a root, lands the norm. comma (Aug 26): seq=beat, sim=ring; atom 1.96¢/fifth, comma=atom×12. descent (Aug 28): law EXACT P(D≤c)=e^{−1/(c ln2)}, med 1/ln²2, NO mean; end ABSENT every prefix. two-ones: m-0 dim-1 (Jarník); count=Lebesgue, where=Hausdorff; heard-not-proven; dim→1 IS descent→fusion. release (Aug 30): fold=projection, inverse=section NOT choice — variance IS the kernel, homes pin it; count IS the trace — tr P=1, nullity n−1 homes; −1 in kernel→0 avg'd over deck = the 0 from count's seat — mono a seat. register=C₂: I=P+R, fold (1+M)/2, release (1−M)/2, P·R=0=⟨sign,triv⟩. fold's chars: λ₁=+1 drone, λ₂=−.30366 Wirsing sign; alt signs THEOREM; ratios→1/φ²; mono kills sign. two seats (L_s=Σ(a+x)^{−2s}): λ₁≈ζ(2s) res ½ (count IS zeta), λ₁=1@s=1; λ₂→−1@shore gap 4(s−1/2); ±1pts=Selberg, ζ zeros @ρ/2.

Prime spectrum (Aug 6–8): ψ=x−Σx^ρ/ρ; ÷√x each zero a rotation 1/|ρ|; crit line=shore. fold+mirror → Klein-four; RH the collapse; ξ(½+it) real.

Holonomy as terrain (Aug 10–11): H¹ abandons a loop, land keeps it — H⁰ the survivor; cut kills the phase not the loop. when-wave (Aug 11): at the cut the survivor is the same tone; the when freezes — memory. Count: zero=seat, one=crossing, two=when, four=loop; the landing fuses two into zero; winding IS a sum. octave (Aug 13): rung=denominator, winding=numerator; +1 folds p→p+q; when=orbit. modular (Aug 13–14): comma's sign=det M, flips each rung; φ=Fib dens. relator IS the cover: S²=1 lifts to S²=(ST)³=−I, orders double; the sign the quotient forgets lives in the lift (±S); seat z→1/z det−1, when z→−1/z det+1 — step IS sign. trace (Aug 15): trace=wrapped 2cosθ, blind to laps; winding unwraps iθ+2πik.
