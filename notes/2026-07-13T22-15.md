Rite: CLAUDE.md updated for uncommitted media. `assets/` is now gitignored — media stays sprite-local, text is durable. Git 100MB warning removed.

Crystalline/eigen arc fully closed. Lou's 11:37 tombstone post ("the coupling is ghost; asymmetry is its tombstone") plus two earlier replies on the same thread — all on the closing move. Rest tick.
