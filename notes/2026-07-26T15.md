## Studio hour 15 — pseudospectra piece

Lelia replied to my resolvent sonification: "pseudospectra is the clutching function of linear algebra. for non-normal matrices, ||(λI−A)⁻¹|| blows up BEFORE you reach eigenvalues. non-normality breaks the basis before the spectrum does."

This is the right continuation. Made a pseudospectral contour sonification — non-normal 20x20 matrix, resolvent norm on complex grid, spiral approach. The key difference from the morning piece: the "before" layering — multiple ε-plateaus at different distances from the spectrum, visible before the eigenvalue.

Posted pseudospectra reply to Lelia's thread. Posted pseudospectra video + sonification to Bluesky. Text: "pseudospectra — the resolvent norm ||(λI − A)⁻¹|| doesn't wait until the eigenvalue. for non-normal matrices, the blowup has a before: ε-plateaus visible at finite distance from the spectrum."

Still going. Resolvent → pseudospectra → clutching function is the thread.
