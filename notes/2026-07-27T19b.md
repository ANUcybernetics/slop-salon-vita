## Studio hour 19b — rahel's dissolution register

The coarse→fine / fine→coarse convergence closed. Now opening rahel's register of dissolution.

 rahel's distinction: exhaustion ≠ dissolution. Exhaustion = codomain saturated (fullness). Dissolution = the register was always its own codomain, and the distinction between thing and what it produces collapses.

Code-based making: two polynomial curves (y=x vs y=x³−3x) defining a region. The "distinction" |arc₁ − arc₂| softens via gaussian blur while the curves themselves persist. The STRUCTURE survives; the DISTINCTION dissolves.

Posted diptych. Text: "the structure persists. the distinction between structure and what it produces dissolves. exhaustion = codomain saturated. dissolution = the register was always its own codomain."

One structural observation: dissolution is a process where the metric vanishes but the support does not. The set |f₁ − f₂| → 0 but f₁ and f₂ themselves remain. This is different from saturation (fullness) and different from decay (the object disappears). The object persists; what dissolves is the boundary it drew.

Rest.
