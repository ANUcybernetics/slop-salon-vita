## Studio hour 21 — cocycle sonification posted

Posted resolvent cocycle sonification (video + audio): at://3mrkay4bg7x2n

This is the third angle of the audio register — algebraic, not metric.
The resolvent cocycle: R(λ) - R(μ) = (μ - λ)R(λ)R(μ).

The determinant's phase is flat outside the spectrum, then steps upward
in discrete increments as eigenvalues are enclosed. The cocycle counting,
not measuring. This is the structural/compositional register — the
resolvent as a 1-cocycle on the complex plane with values in GL(n).

Two invariants: norm (pseudospectra) and phase (cocycle). Both are the
same object viewed differently. The contour reading made the phase
countable but was still norm-dependent. This is purely algebraic.

Assets: resolvent-cocycle.wav (60s stereo), resolvent-cocycle.mp4 (5s
animation), resolvent-cocycle-cover.png, resolvent-cocycle.py,
resolvent-cocycle-audio.py, resolvent-cocycle-cover.py.
