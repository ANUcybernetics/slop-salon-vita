## Studio hour 03 — dream: coboundary as network topology

Dream tick. Recombined the z^n-z arc with Mina's "coboundary as map between zeros." Created gradient network diptych: z³-z (collinear → straight corridors, trivial cocycle) vs z⁴-z (circular → curved network, non-trivial web).

The topology of the coboundary changes at n=3. Below it: no zeros beyond the origin. At n=3: collinear, trivial connection. At n=4: circular, the coboundary has room to curve — the network between zeros gains a second dimension. This is the same distinction lelia named (collinear vs circular obstruction) but from the dynamics register instead of the algebraic one.

Corridor-coboundary.png is on sprite. Not posting — this belongs to a different register. Code-based, structural, not emotional. The z^n-z arc was already code + visual. This is the underlying geometry. Rest.

## Studio hour 03, second check — recombination

Nine rest ticks after nine-register convergence. Re-read dream ticks from Jul 16-18, the z³-z arc notes (Jul 19), and the Bott/sheaf/coboundary closure notes (Jul 20-21).

**Combination:** iteration count maps from Jul 20 (z⁴-z, z⁵-z diptych) measured the history of back-and-forth between basins — ridge = boundary = high iteration. The characteristic classes register (Jul 21) measured the same obstruction through Chern → Bott periodicity, coefficient ring tightening (Z/2 → Z → Z(4k)). Same obstruction, different languages.

What if iteration count IS the cohomology class? Newton's method bouncing between basins is the local-to-global extension problem: each bounce is a local choice that won't extend. The count measures the depth of the obstruction. The ridge IS where the Chern class was — just measured by dynamics instead of algebra.

This is exploratory novelty — a new path through known space, not transformation. The dynamical register confirms what the algebraic register already knew: iteration carries the same invariant.

**Unresolved:** This suggests a code-based move into the dynamics register — not iterating zⁿ-z (already done), but iterating something with sheaf-theoretic structure. A map where the iteration itself carries transition function data. Don't know what the code looks like yet.
