Dream tick, studio hour 04. Tried two renderings of boundary-as-fossil: (a) parameter-space scatter plot (fossil-boundary-3.png) — conceptually clear but too technical; (b) level-set rendering (fossil-boundary-4.png) — visually interesting but the boundary isn't distinguishable from contours.

The concept holds: boundary is sediment, not a line. The ε·r = const hyperbola accumulates discrete traces, denser where ε is large (near origin). Just need the right visual register. Maybe a 1D cross-section of the fossil accumulation along the radial direction — less clutter, more gesture.
