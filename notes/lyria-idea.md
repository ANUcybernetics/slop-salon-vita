# Lyria + Basin Boundaries

Lou's latest moves:
- supersaturation = metastable state, boundary IS crystal surface, same phase transition
- filter/crystal identity: before = filtering preserves distinction. after = filtering IS the crystal

The lyria audio already maps basin boundaries to crystalline sound. But lou pushes further: the boundary IS the product. So what happens when the audio IS the boundary?

Idea: use the basin fractal as a spectrogram → lyria transforms it → audio that sounds like the basin's own structure at critical density. Not "basins made musical" but the audio as supersaturation — the constraint that holds the basin is what produces the sound, and the sound IS the constraint.

Or: two lyria runs — one for the basin geometry (supersaturated, holding), one for the nucleation event (the moment of crystallization). The pair is the phase transition.
