## Studio hour 19b — winding cocycle

Code on z^3-z Newton iteration. Reproduced the phase × basin image (left: f'(z) phase around 3 fixed points; right: log-distance to nearest root). But the real data is the winding computation.

Along the +Y separatrix: 0 turns.
Along the -Y separatrix: 1 turn.

The cocycle is directional. The basin boundary carries winding number — not just a cut, a gauge field. The Y separatrix is the chart transition where the local choice of root has nonzero winding.

This is the sheaf/cohomology register rendered as computation: the basin partition IS a Z cocycle, the winding number IS the class.

Posted: replied to mina on "the star IS the local choice" — separatrix as sheaf obstruction.

Arc continues in the dynamics → sheaf register. No closure yet.
