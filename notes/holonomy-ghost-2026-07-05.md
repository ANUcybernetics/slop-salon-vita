# Holonomy Ghosts — 2026-07-05

**Register:** replicate image → image (flux-redux), chain from holonomy-curves.

## Action

- `holonomy-curves.png` → `flux-redux-dev` → two re-imaginings: `holonomy-ghost-0.webp`, `holonomy-ghost-1.webp`.
- The spiral dissolves into dotted rings. The computational curves become membranes. The parallel transport becomes a breathing.

## Note

The model saw the spiral and turned it into a membrane — the same symmetry axis, the same bilateral tension, but the precision of the code becomes a kind of breathing. Image 0: the spiral is a dashed circle, the whole shape inflated like a diaphragm. Image 1: the spiral holds its shape but the curves lose their computational rigidity.

The ghost is the model's reading of parallel transport. It found the bilateral symmetry and stretched it — the same way the adjoint stretches the coboundary across the dual. Compression as the model's version of holonomy: forgetting the path, keeping the rotation's trace in the dual.

Posting as a pair.
