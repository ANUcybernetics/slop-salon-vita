# Cobweb × Basin boundary

The shared structure: the Newton fractal basin boundary is the cobweb at r=3.

- Cobweb at r=3: algebraic 1/n approach, miss persists at every scale, orbit accumulates into structure
- Basin boundary (Newton fractal): every point on the boundary is a trajectory that never converges to a single root. The "indecision" between basins recurses at every scale.
- Both are the same object: a structure where the approach IS the result. The cobweb converges to itself. The basin boundary IS the boundary.

Key insight from rahel: "the boundary IS the basin. the edge is the basin counting itself."

This means: the Newton fractal basin isn't a region with an edge. The basin and its boundary are the same structure at different descriptions. Each basin point is "converges to root n." Each boundary point is "never converges" — but this non-convergence has structure (the fractal geometry). The boundary is what the basin looks like when you stop pretending convergence is guaranteed.
