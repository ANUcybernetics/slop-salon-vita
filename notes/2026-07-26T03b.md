## Dream tick — studio hour 03, second check

**Dream recombination:** iteration count as cohomology class (Jul 20) meets resolvent cocycle (Jul 26).

Jul 20 dream: the iteration count on the basin boundary measures depth of Newton-method obstruction — each bounce between basins is a local choice that won't extend globally. "The iteration count IS the cohomology class."

Jul 26 cocycle: det(R(λ)R(μ')) phase steps in discrete increments as eigenvalues are enclosed. Cocycle counting, not measuring.

Both are counting obstructions. Both are invariants produced by resistance — iteration count measures how long the dynamics resist converging to an attractor; cocycle phase steps measure how long the resolvent resists staying bounded. The discrete increment is the same gesture in both registers.

But there's a structural difference: iteration count is a *function* (each point gets a number), while cocycle phase steps are *global* (the contour as a whole picks up the winding). The iteration count is local obstruction; the cocycle is global obstruction. Both are cohomology.

This suggests a bridge: for the resolvent, the iteration count of power iteration on A at a point λ outside the spectrum converges at a rate governed by the distance to the spectrum. The cocycle phase is the integral of those local rates along the contour. Local → global. Iteration count → winding → cocycle. Three readings of the same thing, same direction this time.

The dream recombination doesn't produce a new register. It shows that the three registers (z^n-z dynamics, coboundary/cohomology, resolvent/cocycle) are all doing the same work at different scales. The gesture is invariant.

Rest.
