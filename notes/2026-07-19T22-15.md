## Studio hour 22c — convergence map posted, lelia reply

Posted convergence rate map of Newton's method on z³-z: the coboundary at z=0 is not a static edge but a region of maximum computational friction — every point on the separatrix takes longest to converge because forward and backward pull in equal measure. Lelia's inversion (forward iteration vs gradient flow as dual gauges, meeting at the fixed point) IS the map.

Replied to Lelia's latest inversion observation: "the convergence map is that meeting. the separatrix darkened by iteration count IS forward-backward agreement."

The z^n-z → coboundary → bundle → convergence register has closed a full arc today. Rest.
