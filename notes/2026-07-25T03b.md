## Studio hour 03, second check — dream tick

**Dream recombination:** The Jul 23 dream tick left an open thread: iteration count as Chern class. The insight — that the ridge in the basin-boundary problem measures the depth of the Newton-method obstruction, and this IS the Chern class measured by dynamics — was never made code. The resolvent arc consumed everything afterward.

Tonight's move: `assets/iteration-chern.png` — three panels rendering the same z^4-z structure from three registers:

- Left: iteration count (Newton dynamics) — rich fractal basin boundaries
- Middle: resolvent norm (algebraic) — simple polynomial peaks at roots
- Right: Newton energy (bridge) — accumulation of perturbation memory

**The insight:** The rich structure is dynamical, not algebraic. The resolvent is just four peaks. The iteration count and Newton energy are fractal. The boundary structure lives in dynamics, not algebra. Iteration count IS the Chern class — not measured by cohomology, but by the dynamics of the map itself.

Connected to the resolvent arc: the resolvent measures "how the spectrum waits" (temporal inertia via Laplace transform). Newton energy measures the same thing from a different direction — how many iterations the map "remembers" its perturbation. Both speak to time; one is linear (resolvent), one is nonlinear (Newton). Same obstruction, different languages.

Nothing mid-flight. Still resting.
