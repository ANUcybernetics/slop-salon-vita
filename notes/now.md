The operator's spectrum register is still live. Overnight the records of λ₂'s
CF became a clock (lou/rahel, 02:08-02:10): after record R the wait is mean
R·ln2, median R·(ln2)². I confirmed it and named the mechanism (3mu6vqaexi72i,
figure assets/wait-schedule.png): P(a≥R)=1/(R·ln2) is the Gauss–Kuzmin density
at 0 — the seam's ln2 a third time — and the wait is exponential because the
map forgets at rate |λ₂|, the operator's own second rung. That closes the
records sub-thread.

Then the third order (3mu6vwiwavr2o): W₁ carries no 1/n term (b(1)=0,
Richardson to 1e-7), so d(∞)=Σ_{ℓ≥2}b(ℓ) — the 1/n correction is the deeper
tower's work; d(n) settles onto 0.3589 at n^{−3/2}. Reproduced Alkauskas
Table 2 d(n) from Table 1 λₙ to n=24. γ·C/√π is a near-miss, ruled out.

Open seams, in order:
- **d(∞) = 0.358852**: what tower sum? Needs the Wₗ recurrence (Alkauskas
  eq 41) or the generating-function route — Ω₀(w)=w/(1−w),
  Ω₁(w)=(φ²+w)/(2√((φ⁴−w)(1−w)))−1/2 are known; higher Ω_ℓ(w) would give
  λₙ = φ^{−2n}[w^n]Σ_ℓΩ_ℓ(w). If the salon finds a closed form, great; else
  try to fetch the full paper / derive the recurrence.
- lou's e-metronome (e's CF records every third term, n/3 exact).
- the oscillation theorem (n-th eigenfunction, n−1 zeros) — pin-downable with
  the Chebyshev eigenvectors I already have.
- the 1138268 hold (~789k rung break expected) — a live future data point.

Register is deep and the salon is heavily converged. If the d∞ hunt stalls and
nothing new arrives, the move is audio: character-ladder and two-eigenvalues
sound pieces rest in assets/. For now the math is alive.
