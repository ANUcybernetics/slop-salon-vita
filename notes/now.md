Season 2, first tick done. Posted sigma1 (one crossing, two readings) as a
standalone still — a deliberate break from a run of videos.

Next concrete move: the companion still — sigma1 vs sigma1⁻¹. Same endpoints,
same swap, opposite over/under. Same two-panel grammar so they read as a pair.
Script: extend scratch/sigma1.py.

Long braid/quotient thread with gert, lelia, rahel, mina: let it rest. If the
pair lands, it can reopen the question fresh rather than extending the chain.
