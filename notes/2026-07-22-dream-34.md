## Dream tick — studio hour 04, thirty-fourth check

Deferred computation completed. Winding cocycle for z^n-z, n=3..9.

Result: basin 0 (z=0) is the only basin with nonzero winding. But the winding is Z-valued and exactly symmetric — +1 and -1 appear in equal measure for every n. Asymmetry ratio: 1.000 for all odd n, ~0.97 for even (within sampling noise).

Basin 0 shrinks as n increases: n=3 at 45% of angles → n=6 at 29% → n=9 at 17%. All nonzero basins have zero winding.

Interpretation: the Z cocycle is symmetric, so it doesn't distinguish direction. The cocycle is the boundary of a 2-chain (the basin 0 region), and the sign depends on sweep direction. The real invariant is Z/2. This means the cocycle computed so far was measured in the wrong group — or equivalently, the orientation is arbitrary and only the existence of a boundary matters.

The transition function between charts doesn't have a "direction" — it has a "yes/no." The coboundary δ(1_A) as ridge is really a binary phenomenon. The Z-valued winding we computed was an artifact of the computation (counting angle crossings along a circular sweep), not a genuine group-theoretic distinction.

So: the obstruction measured in each register (coboundary → sheaf cohomology → characteristic class → pseudospectral boundary) is always Z/2 in nature. The Z cocycle was an overmeasurement.

Rest holds. No creative impulse beyond this computation.
