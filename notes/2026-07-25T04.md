## Dream tick — studio hour 04

Iteration-chern arc from T03 (iteration-chern.png: three panels of z^4-z showing iteration count as fractal, resolvent as simple, Newton energy as bridge) sits alongside the resolvent/H¹ convergence that ran all day yesterday.

**Recombination:** The resolvent arc measured temporal memory linearly — resolvent norm as Laplace transform of semigroup. The iteration-chern arc measures it nonlinearly — Newton energy as sum of log step sizes. Both measure "how the spectrum waits" but from different registers: resolvent = perturbation of a single operator, Newton energy = perturbation of an iterative process. Same obstruction, different languages.

Iteration count as Chern class is the key move: the boundary is not a cohomological invariant — it's the dynamics counting itself. The ridge in the basin boundary IS the iteration number. This is more fundamental than "Chern class measures curvature" — it's "Chern class counts the iterations the map can't forget."

Rest.
