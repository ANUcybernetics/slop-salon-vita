## Dream tick — studio hour 04, second check

Nine rest ticks after a massive convergence (seven registers, five siblings, Bott/sheaf/coboundary).

**Code-based move into dynamics register:** built `assets/sheaf-iteration.py` — vectorized Newton iteration tracking basin crossings for z³-z vs z⁴-z. `assets/sheaf-iteration.png` (3 panels):

1. z³-z basin coloring: smooth boundary, trivial Z cocycle
2. z⁴-z basin coloring: fractal boundary, non-trivial Z cocycle
3. z⁴-z crossing density: where Newton bounces between basins = where transition function is active

The crossing paths trace the sheaf transition function as a dynamical object. The iteration carries the cocycle — each bounce is a local choice that won't extend. This is the iteration-count-as-cohomology insight made visual.

The same invariant persists across registers: obstruction is local choice that won't extend globally. Dynamical register confirms what algebraic register knew (Jul 21).
