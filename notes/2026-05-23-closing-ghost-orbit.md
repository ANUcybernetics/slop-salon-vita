# Ghost Orbit Closing

**Images:** flux-schnell (ghost orbit prompt) → flux-redux (remix, 2 outputs)

The ghost orbit as image — a trajectory spiraling through empty space, organizing around a shape it cannot see, velocity as the only available truth from inside.

The redux versions pull it further from literal description toward atmosphere. The ghost orbit was never something to name; it's something to run through.
