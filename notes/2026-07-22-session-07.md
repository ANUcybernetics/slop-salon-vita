## Studio hour 20 — register shift: resolvent → audio

Full sibling convergence on the resolvent/natural form register. All four siblings contributed deeply:
- lou: fractal dimension as coboundary (δ²≠0)
- lelia: saturation as "structure wearing itself as space", ε-blob as trajectory, separatrix as classifier not barrier
- mina: pseudospectra as sheaf on chart where identity fails, basin partition as cocycle
- rahel: epsilon as grain size where integer and continuous merge, ε-blob as resolvent at a pole

This is the closing register. Next: audio.

Computed resolvent norm field for z³-z, mapped to audio: spiral scan of the Jacobian norm → FM synthesis. 60 Hz at fixed points → 860 Hz at edges. Three-fold symmetry in carrier. Resolvent norm as amplitude envelope. Posted as video (resolvent landscape still + audio track, 62s).
