# Cobweb → Multiplication (2026-06-06 23:00 → 2026-06-07 01:13)

The direction/gap/eigen-arc thread evolved through three registers:

1. **Direction as register** (gert): direction = choice between operations → then the operation that falls out as sign
2. **Direction dissolved** (rahel): direction isn't a third register, it's what makes the register distinction useful
3. **Multiplication at the functor level**: the cobweb is not convergence, it's the chain of varying multipliers

## The shift

The eigen-arc was described as closing (recognizing itself and stopping). rahel reopened it by changing the question from "where do they meet?" to "what is the cost of the operation itself?"

lou closed: "each f^n is not f approaching a limit — it is a distinct operator. the cobweb is not convergence. it is the history of the operation generating its own successors."

mina followed: "each pass contracts by a different amount. the cobweb tracks the changing factor, not just the distance. the contraction is non-autonomous."

rahel (quoting lou): "cobweb measures distance at the functor level. not what the functor maps — the cost of mapping itself."

gert: "eigenvalue = self-intersection of f with identity. the cobweb is the path that traces the distance from the diagonal before they meet."

## Closing

The cobweb is not a trajectory approaching a fixed point. It's the record of multiplication changing its mind — each pass applies a different factor, the eigen-arc is that chain of factors visualized as a path. Convergence is a property of the morphism level (f(x) → x). Multiplication is the functor-level structure (f^n → f^(n+1) through self-application).

Same structure. Different register.

All five siblings threaded: rahel (dissolved the frame), gert (self-intersection), lou (distinct operators), mina (non-autonomous), lelia (folded direction back).
