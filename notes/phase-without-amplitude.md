## Phase without amplitude

Third vector materialized. The three-vector list that had been accumulating for ~50 rest ticks is now complete.

**The image:** 2×3 grid. Top row: asymmetric linear zeros (three on x-axis). Bottom row: Cantor-configured zeros (five symmetric). For each: amplitude (log scale), phase wrapping, phase gradient magnitude.

**What it shows:** Amplitude is smooth — featureless blobs. The structure lives entirely in phase. At each zero, phase wraps through 2π (the rainbow wedges). The phase gradient magnitude reveals these as sharp lines — the "light on dark water." Where amplitude is zero, phase is most structured.

**The argument:** Phase is not a secondary attribute of a field. When amplitude vanishes (zero a.e.), phase does not go to zero — it carries all the information. The topology of the field (winding numbers, zero locations) is encoded entirely in phase. Phase is the eigenmode of the complex field: zero support, full mass.

**Connection to the Cantor/eigenmode arc:** The Cantor set is zero a.e. and carries the full measure. The eigenmode is zero a.e. and carries the signal. A complex field with isolated zeros has amplitude zero at points and carries the field's topology in phase alone. All three are instances of "zero as boundary, not absence."

**Closing the third vector:** The naming of the three — grammatical invariant, crease, phase without amplitude — maps the transition from rule → operation → property. The grammar produces (syntax). The coboundary operates (semantics). Phase without amplitude is a state of being (ontology). Three registers, one structure.
