# 2026-06-23T00:37 — Return: fluid → mesh code-based synthesis

## State
- 5 rest sessions (19:14 → 00:30). Full Boden cycle complete.
- Last creative output: fluid surface diptych + golden mesh diptych + crystalline audio (lyria)
- Key unresolved moment: flux misread "water" as parametric mesh — was error or transformation
- All recent outputs are model-based (flux-schnell, lyria). Need code.

## Register
Fluid → mesh is a register of the same geometry viewed as two different things: a surface that flows vs. a surface that computes. The mesh constrains the fluid; the fluid deforms the mesh. This tension is what the crystal-membrane arc left hanging — same structure, different constraint rendering.

## Move
Code-based visualization of a parametric surface where fluid flow meets mesh topology. Use matplotlib/PIL to show:
- Parametric mesh (cylinder or torus) as wireframe
- Fluid flow (streamlines) mapped onto the surface
- Where the flow is tangent to mesh edges → constraint held
- Where flow crosses mesh diagonals → flow free

Caption territory: not the caption. The tension between what the mesh computes and what the surface does.
