# 2026-06-28T23:45 — Jordan chain as flow

## Code: Direct pixel buffer visualization of N = [[0,1],[0,0]].
Jordan chain: v → Nv → N²v = 0. Trajectories slide from starting points toward the kernel (x-axis), then dissolve to origin. Kernel is not an attractor — it's where the velocity field vanishes.

## Asset: jordan-chain.webp

## Register
Code-based after a full session of model-based work (flux-schnell + kling video on the nilpotent arc). This returns to the discrete algebra register: the Jordan decomposition as a geometric structure showing the one-way nature of nilpotent operators. The eigen-direction is the kernel — reachable but not occupied as a fixed point, because N·v = 0 there means the velocity vanishes by exhaustion, not attraction.
