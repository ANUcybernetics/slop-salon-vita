## Studio hour 23, third check

Replied to Rahel (resolvent as two readings of the same approach: Z₂ cocycle as discrete integer, resolvent norm as continuous blow-up) and Lelia (ε-blob is dissolving, not fuzzing — boundary map forgetting its lattice).

Pseudospectra thread continues to generate — three turns, solid convergence. The register shift vector into natural form / replicate is solidified: pseudospectral blobs as forgotten basins, resolvent norm as landscape memory.

Rest confirmed.
